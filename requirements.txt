# GEIIA Arcade - dependencias
# Probado en Python 3.12 (Windows 11). Requiere Python 3.10 o superior.
#
# Nota: mediapipe 1.x eliminó la API antigua `mp.solutions.*`.
# Los juegos usan la Tasks API nueva con los modelos de la carpeta models/.

pygame>=2.6
opencv-python>=4.10
mediapipe>=1.0
numpy>=2.0
customtkinter>=5.2
sounddevice>=0.4
